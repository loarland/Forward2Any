# 运行时数据（SQLite 库与 WAL 文件）
/data/
/.tmp/
*.db
*.db-wal
*.db-shm

# 本地编译产物
/f2a
/dist/

# 编辑器 / 系统
.DS_Store
.idea/
.vscode/

# Python（scripts/ 下的辅助脚本）
__pycache__/
*.pyc

# 只在本地留存的设计 / 开发文档：内容细到每一步的取舍和实测数据，不进仓库。
# 想改内容直接编辑 internal-docs/ 下的文件，git 永远看不到它们。
/internal-docs/
